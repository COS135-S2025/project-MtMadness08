#ifndef STRUCT_H
#define STRUCT_H

#include "Questions.h"

typedef struct Structs{
    struct Question** Text;
    int num_questions;
    
} Structs;

Structs* createArray(int num){ //Creates the arrayv

}

void freeArray(Structs* aStruct){ //Frees the array
}

void addSet(Text* set, Structs* aStruct){ //Adds the set
}

#endif